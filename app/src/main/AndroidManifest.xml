<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ng.insitu.mmuta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/nums_label" android:name=".GoToNumbers" />
        <activity android:label="@string/phrases_label" android:name=".GoToPhrases" />
        <activity android:label="@string/fam_label" android:name=".GoToFamily" />
        <activity android:label="@string/objs_label" android:name=".GoToObjs"></activity>
    </application>

</manifest>